{
  "name": "bug-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix backend",
    "install": "npm run install-server",
    "server": "npm run --prefix backend",
    "watch": "npm run server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "note": "&& run and waits to finish & run both without waiting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
